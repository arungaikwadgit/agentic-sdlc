# DEPRECATED — DO NOT USE
#
# This file predates the backend/frontend → Agent Runtime split and is no longer
# referenced by anything in this repo, CI, or documentation. The active compose
# file is the one at the repo root (../../docker-compose.yml), which defines the
# db, proxy, and runtime services.
#
# This file could not be deleted automatically — file deletion was blocked at
# the filesystem mount level during this session (see remediation report for
# details). Please delete this file manually:
#
#   rm docker/docker-compose.yml
