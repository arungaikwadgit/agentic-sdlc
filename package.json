{
  "name": "agentic-sdlc",
  "version": "1.0.0",
  "description": "Agentic SDLC Framework — root workspace",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "cd shared-types && npm install && cd ../backend && npm install && cd ../frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
